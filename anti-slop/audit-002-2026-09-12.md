# Antislop follow-up: approved findings 1–6

User approved all findings from audit-001. Source changes address each item.

- 1 PASS: dock hover renders at opacity 1.0, preserving contrast while expanded; resting opacity remains configurable.
- 2 PASS: collapsed dock is a focusable Button; Enter/Space expands it. Arrow keys move dock offset. Escape collapses. Drag remains available for pointer users.
- 3 PASS: Render captures current keyboard focus and schedules focus restoration after rebuilding controls.
- 4 PASS: usage refresh shows `Mengemas kini baki usage…`; manual update checks show `Menyemak update…`.
- 5 PASS: generic diamond glyph removed from dock/header labels. `CODEX`, `CODEX / USAGE`, and `UPDATE` describe function.
- 6 PASS, automated scope: source builds; usage/release/geometry tests pass; WPF smoke checks cover all four edges, floating restore, default/custom opacity. The MSI installer and live browser login still require an interactive desktop check before a human release claim.

Evidence rerun: `dotnet build src/CodexUsage.csproj -c Release`, `dotnet run --project tests/Tests.csproj`, and `dotnet run --project verification/Smoke.csproj`. No live pointer/keyboard or MSI upgrade test was available in this audit environment.

Remaining limitation: at very low resting opacity, the collapsed tab itself is intentionally translucent. Expanded content is opaque for readability. Users can set a higher opacity through **··· → Opacity**.
